import React from "react";
import { AppBar } from "material-ui";

const NavBar = () => {
  return (
    <AppBar
      title="App Fun"
      option="f"
      style={{
        backgroundColor: "#5DBAFB"
      }}
    />
  );
};

export default NavBar;
